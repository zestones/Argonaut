
var i : int;

func fibonacci(num : int) -> int {
    if (num <= 1) { 
        return num; 
    } else {
        return fibonacci(num - 1) + fibonacci(num - 2);
    }
}

for (i := 0; i < 20; i++) {
    print("fibonacci(%d) = %d\n", i, fibonacci(i));
}
