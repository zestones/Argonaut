PROG

var x : int;
var x : float;

type x : array[0:10] of int;

type x : struct 
{
    jour : int;
    mois : int;
    annee : int;
} fstruct;

func main(a: int) -> int {
    return 1;
}

proc main(a: float) {
    var z : x;
    z[0] := 1;
    // a := 3.14;
}