PROG
var entier : int;

type tableau_entier : array[0:10] of int;
type date : struct {
    jour : int;
} fstruct;
 
func my_function(a:int) -> int {
    var today : date;
    today.jour := 1;
    return today.jour + a;
}

entier := 2;
entier := my_function(entier);
