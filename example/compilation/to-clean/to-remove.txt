PROG

var x : int;

type x : array[0:10] of int;

proc x(a: float) {
    var z : x; 
    z := 1;
    // a := 3.14;
}

// change in assignment_validation.c the message to :
// (variable_type == NULL_VALUE) ? "UNKNOWN" : get_lexeme(variable_type) message should display
// '' instead of the actual type of the variable