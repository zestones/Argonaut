PROG

type arr: array[0:1] of int;
type date : struct {
    day : int;
    month : int;
    year : int;
} fstruct;

var x : int;
var y : int;
var arr : arr;
var date : date;

x := 0;
y := 0;


input("%d %d %d", x, arr[1], date.day);

print("--> %d -- %d -- %d\n", x, arr[1], date.day);