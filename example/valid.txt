PROG

var x : int;

proc p1(x : float, y : float) {
    x := 3,5 ;
} // fin de la procédure p1

proc p2(a : int, b : int) {
    type x : struct {
        ch1 : int;
        ch2 : int;
    } fstruct;

    var s : x;

    proc p3(c : int) {
        func x(c : float) -> int {
            var a : float;

            return 0;
        } // fin de la fonction x

        proc g(x : bool) {
            var c : int;

            c := 88;
        } // fin de la procédure g

        c := x(3,14);
    } // fin de la procédure p3

    proc p4() {
        var x : x;
        var c : char;

    } // fin de la procédure p4

} // fin de la procédure p2

proc p5(a : int, c : int) {
    var x : float;

    x := 2,718;
    c := 77;
} // fin de la procédure p5

x := 42;
p2(x, x);
// fin du programme principal